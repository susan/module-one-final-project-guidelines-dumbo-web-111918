#category(id: integer, inward: boolean, outward: boolean, onward: boolean, vocal: #boolean, instrumental: boolean)

def type(song, category)

# @@Song.all.select do |song|
#   if song.category == inward && song.category ==vocal,
#    song_categories.type = inward_vocal
